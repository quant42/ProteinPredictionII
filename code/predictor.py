#! /usr/bin/env python

# this is the predictor function, that runs the prediction for a graph

# imports
import pickle, features

class Predictor():
  
  """ The predictor for a node """
  
  def __init__(self, netFile):
    
    """ Initalize a predictor with a querySequence and the corresponding graph """
    
    self.querySequence  = querySequence
    self.graph = graph
  
  def runprediction(self, querySequence, graph):
    pass
